# Biblioteca web
fastapi>=0.110.0

# Servidor ASGI (para uvicorn)
uvicorn[standard]>=0.30.0

# Base de datos SQL (ORM)
sqlalchemy>=2.0

# Conector para PostgreSQL (si usas Docker + PostgreSQL)
psycopg2-binary>=2.9

# Variables de entorno (para .env y Docker)
python-dotenv>=1.0

# Opcional: herramientas durante el desarrollo
pydantic>=2.0